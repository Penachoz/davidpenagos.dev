# CompuNube

Curso / laboratorio de **computación en la nube** (Universidad Autónoma de Occidente).

Este repositorio guarda prácticas. Cada carpeta `Practica_N` debería explicar: objetivo, proveedor o hipervisor, comandos, y qué se destruye al terminar (costo y leftovers).

## Estado

La primera práctica está en `Practica_1`. Este README evita que el repo se vea vacío en el perfil.

No está pineado hasta que haya un laboratorio reproducible (VM o contenedor) con instrucciones de `up` / `destroy`.
